# 🚀 下一站：GraphRAG 实战

## 🚩 当前状态
- [x] **Chapter 2.1 - 2.2 完成**：已实现并验证了向量检索、HyDE、LLM Rerank 及其组合模式。
- [x] **知识沉淀完成**：建立了向量索引原理、重排技术复盘等深度文档。
- [x] **评测基准建立**：`benchmark_rag.py` 已成为后续改进的度量衡。

---

## 🎯 即将开启：Chapter 2.3 - GraphRAG (知识图谱增强)

### 1. 核心任务：从“点”到“网”
目前我们的 RAG 只能处理语义相似度（点对点），无法处理逻辑链条（如：A 是 B 的上游，B 导致了 C）。下一步我们将通过图结构来补全这一环。

### 2. 实战清单
- [ ] **环境搭建**：安装 `networkx` 或配置轻量级图存储。
- [ ] **实体提取 (Entity Extraction)**：利用 LLM 从现有 `dataset.py` 中提取三元组 (Subject, Predicate, Object)。
- [ ] **图检索实现**：编写 `graph_search` 函数，能够根据关键词跳跃查询关联实体。
- [ ] **全链路评测**：在 `benchmark_rag.py` 中新增 `GraphRAG` 模式，对比其在复杂逻辑题上的表现。

---

## 💡 给下次的自己
**启动指令：**
> “读取 `NEXT_TASK.md`，开始 Chapter 2.3 的 GraphRAG 实战准备。首先帮我分析如何从现有的语义混淆数据集中提取出有价值的关系图谱。”

---
*上次更新：2026-04-27*
